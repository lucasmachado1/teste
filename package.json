{
  "name": "guardian-chain-watch",
  "version": "1.0.0",
  "private": true,
  "description": "Painel seguro para monitoramento em tempo quase real de endereços cripto autorizados.",
  "scripts": {
    "dev": "node scripts/patch-postcss-exports.js && NODE_OPTIONS=--openssl-legacy-provider next dev",
    "build": "node scripts/patch-postcss-exports.js && NODE_OPTIONS=--openssl-legacy-provider next build",
    "start": "NODE_OPTIONS=--openssl-legacy-provider next start"
  },
  "dependencies": {
    "next": "^10.1.3",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14"
  }
}
